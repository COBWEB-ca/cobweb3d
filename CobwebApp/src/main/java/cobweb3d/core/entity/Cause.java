package cobweb3d.core.entity;

/**
 * This interface intends to illustrate the reason why an agent's energy is changed.
 * It hasn't been used in current version (0.1.4)
 */
public interface Cause {
    String getName();
}
